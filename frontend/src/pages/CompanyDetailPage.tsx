import{useCallback,useEffect,useState}from"react";import type{ApiClient}from"../api/client";import type{CompanyDetail}from"../types";import{ErrorState,LoadingState}from"../components/PageState";import{Icon}from"../components/Icon";import{StatusBadge}from"../components/StatusBadge";
/* eslint-disable react-hooks/set-state-in-effect -- load() also resets state for explicit retries. */
export function CompanyDetailPage({id,client,navigate}:{id:string;client:ApiClient;navigate:(path:string)=>void}){const[company,setCompany]=useState<CompanyDetail|null>(null),[error,setError]=useState(false);const load=useCallback(()=>{setError(false);setCompany(null);void client.getCompany(id).then(setCompany).catch(()=>setError(true))},[client,id]);useEffect(load,[load]);if(error)return <><button className="back-link" onClick={()=>navigate("/companies")}><Icon name="back" size={18}/>Back to companies</button><ErrorState message="This company could not be found or loaded." retry={load}/></>;if(!company)return <LoadingState label="Loading company…"/>;return <section><button className="back-link" onClick={()=>navigate("/companies")}><Icon name="back" size={18}/>Back to companies</button><div className="page-heading detail-heading"><div><div className="title-row"><h1>{company.name}</h1><StatusBadge status={company.status}/></div><p>{company.officialName}</p></div><button className="button button--secondary">Edit company</button></div><div className="detail-grid"><article className="panel"><div className="panel-title"><div><p className="eyebrow">Profile</p><h2>Basic information</h2></div></div><dl className="info-list"><div><dt>Official name</dt><dd>{company.officialName??"—"}</dd></div><div><dt>Email</dt><dd>{company.email??"—"}</dd></div><div><dt>Phone</dt><dd>{company.phone??"—"}</dd></div><div><dt>Address</dt><dd>{company.address??"—"}</dd></div><div><dt>Website</dt><dd>{company.websiteUrl?<a href={company.websiteUrl}>{company.websiteUrl.replace(/^https?:\/\//,"")}</a>:"—"}</dd></div></dl></article><article className="panel"><div className="panel-title"><div><p className="eyebrow">Library</p><h2>Published brands</h2></div><span className="count-pill">{company.brands.length}</span></div>{company.brands.length?<div className="brand-list">{company.brands.map(b=><div key={b.id}><div className="company-monogram">{b.name.slice(0,2).toUpperCase()}</div><div><strong>{b.name}</strong><span>{b.identifier}</span></div><StatusBadge status={b.active?"active":"inactive"}/></div>)}</div>:<p className="muted">No published brands.</p>}</article></div><article className="panel panel--wide"><div className="panel-title"><div><p className="eyebrow">Work</p><h2>Company projects</h2></div><span className="count-pill">{company.projects.length}</span></div>{company.projects.length?<div className="compact-list">{company.projects.map(p=><button key={p.id} onClick={()=>navigate(`/projects/${p.id}`)}><span className="project-number">{p.number}</span><strong>{p.name}</strong><StatusBadge status={p.status}/><Icon name="arrow" size={18}/></button>)}</div>:<p className="muted">No projects for this company.</p>}</article></section>}
