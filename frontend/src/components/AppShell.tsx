import { useState, type ReactNode } from "react"; import { Icon } from "./Icon";
const items = [["Dashboard","/dashboard","dashboard"],["Companies","/companies","companies"],["Projects","/projects","projects"],["Materials","/materials","materials"],["Publication","/publication","publication"],["Settings","/settings","settings"]];
export function AppShell({ currentPath, navigate, children }: { currentPath: string; navigate: (path:string)=>void; children: ReactNode }) { const [open,setOpen]=useState(false); const go=(event:React.MouseEvent<HTMLAnchorElement>,path:string)=>{event.preventDefault();navigate(path);setOpen(false)}; return <div className="app-shell"><aside className={`sidebar ${open?"sidebar--open":""}`}><a className="brand" href="/dashboard" onClick={e=>go(e,"/dashboard")} aria-label="REAWOTE dashboard"><span className="brand-mark">R</span><span>REAWOTE</span></a><nav aria-label="Main navigation">{items.map(([label,href,icon])=><a key={href} href={href} onClick={e=>go(e,href)} className={currentPath===href||(href!=="/dashboard"&&currentPath.startsWith(`${href}/`))?"active":""}><Icon name={icon}/><span>{label}</span></a>)}</nav><div className="sidebar-footer"><div className="avatar">JD</div><div><strong>Jan Dvořák</strong><span>Administrator</span></div></div></aside>{open&&<button className="scrim" aria-label="Close navigation" onClick={()=>setOpen(false)}/>}<div className="workspace"><header className="topbar"><button className="menu-button" onClick={()=>setOpen(true)} aria-label="Open navigation"><Icon name="menu"/></button><div className="environment"><span/>Internal workspace</div><button className="user-button" aria-label="User menu">JD</button></header><main className="content">{children}</main></div></div> }
